# 📰 News Article Recommendation System using Embeddings

This project recommends similar news articles using **text embeddings** and **cosine similarity**.

It answers:

> "If I read this article, which other articles are similar?"

---

## 🚀 Features

- Converts article text into numerical vectors (embeddings)
- Finds similar articles using nearest neighbor search
- Uses cosine distance for similarity
- Saves embeddings in cache to reduce API cost
- Visualizes articles using t-SNE (2D plot)
- Highlights source article and its nearest neighbors
